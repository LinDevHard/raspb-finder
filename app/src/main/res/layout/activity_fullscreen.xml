<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="viewModel"
                  type="com.hackware.mormont.raspbfinder.MainViewModel">
        </variable>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#000a12"
            tools:context=".FullscreenActivity">

        <Button
                android:id="@+id/find_rasp"
                android:text="@android:string/search_go"
                android:contentDescription="@android:string/search_go"
                android:layout_width="139dp"
                android:layout_height="82dp"
                android:textAppearance="@style/TextAppearance.AppCompat.Display1"
                android:onClick="@{()-> viewModel.onClickSearch()}"
                android:layout_marginStart="8dp"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginEnd="8dp"
                app:layout_constraintEnd_toEndOf="parent" android:layout_marginTop="8dp"
                app:layout_constraintTop_toTopOf="parent" android:layout_marginBottom="8dp"
                app:layout_constraintBottom_toBottomOf="parent"/>

        <TextView
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:id="@+id/rasp_text"
                android:layout_gravity="bottom"
                android:visibility="visible"
                android:text="@{viewModel.infoMessage}"
                android:marqueeRepeatLimit="marquee_forever"
                android:textAlignment="center"
                android:padding="16dp"
                android:textColor="@color/primary_text_color"
                android:layout_marginEnd="8dp" app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginStart="8dp"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginBottom="8dp"
                app:layout_constraintBottom_toBottomOf="parent" android:layout_marginTop="8dp"
                app:layout_constraintTop_toBottomOf="@+id/find_rasp" app:layout_constraintVertical_bias="0.229"/>
        <TextView android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:id="@+id/rasp_ip"
                  android:layout_gravity="bottom"
                  android:visibility="visible"
                  android:text="@{viewModel.raspberryIpAddress}"
                  android:marqueeRepeatLimit="marquee_forever" android:padding="16dp"
                  android:textSize="32sp"
                  android:textColor="@color/primary_text_color" android:layout_marginTop="8dp"
                  app:layout_constraintTop_toBottomOf="@+id/rasp_text" android:layout_marginBottom="8dp"
                  app:layout_constraintBottom_toBottomOf="parent" android:layout_marginEnd="8dp"
                  app:layout_constraintEnd_toEndOf="parent" android:layout_marginStart="8dp"
                  app:layout_constraintStart_toStartOf="parent" android:textAlignment="center"
                  app:layout_constraintVertical_bias="0.2"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>